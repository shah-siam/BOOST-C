#include<stdio.h>
int main(){
    float a,b,multiply;
    printf("Enter two numbers: ");
    scanf("%f %f", &a, &b);
    multiply = a * b;
    printf("Multiply = %f\n", multiply);
    return 0;

}